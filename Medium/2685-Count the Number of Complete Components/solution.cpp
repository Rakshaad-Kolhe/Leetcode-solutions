}
        }
                dfs(v, nodes, edges);
            if (!vis[v])
        for (int v : adj[u]) {

        edges += adj[u].size();
        nodes++;
        vis[u] = 1;
    void dfs(int u, int &nodes, int &edges) {

    vector<int> vis;
    vector<vector<int>> adj;
public:
class Solution {